# SOW

## SOW-002

Build a governed `cloudflare` Developer Dashboard skill that can bootstrap Cloudflare Tunnel setup inside a project, including Docker command execution, project `.env` persistence, generated tunnel config, Docker validation, documentation, and release records.
